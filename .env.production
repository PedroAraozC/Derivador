# Config de PRODUCCIÓN (Vite la usa solo en `npm run build`, no en `npm run dev`).
# URL pública del backend de Migue (el del endpoint /api/chat del chat web).
VITE_MIGUE_API_URL=https://migue.smt.gob.ar
